// more recursion practice...

// Get the integers in range (x, y)
// Example : range(2, 9)
// Expected Output : [3, 4, 5, 6, 7, 8]

// base case: x + 1 = y
// recursive case: rangeFinder(x + 1, y - 1)
// error case: x should be < y

const rangeFinder = (x, y) => {

}

console.log(rangeFinder(2, 9));
